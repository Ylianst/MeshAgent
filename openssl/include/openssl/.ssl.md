<!-- source-hash: 57dd18a479285d590f63b08d56363d39 -->
Generated auto-generated OpenSSL SSL/TLS header providing core type definitions, constants, cipher text strings, and session management macros for the OpenSSL SSL/TLS protocol implementation.

## Key Components

### Core Type Definitions
- `SSL_METHOD` — Protocol method structure (TLS/DTLS variants)
- `SSL_CIPHER` — Cipher suite descriptor
- `SSL_SESSION` — TLS session state container
- `SSL_CONF_CTX` — Configuration context
- `TLS_SESSION_TICKET_EXT` — Session ticket extension
- `SRTP_PROTECTION_PROFILE` — SRTP profile (name + ID) for RFC 5764

### Session Constants
- `SSL_SESSION_ASN1_VERSION` — ASN.1 session encoding version (`0x0001`)
- `SSL_MAX_SSL_SESSION_ID_LENGTH` — Max session ID length (32 bytes)
- `SSL_MAX_PIPELINES` — Max encrypt/decrypt pipelines (32)

### Cipher Text Strings (`SSL_TXT_*`)
String constants for cipher selection strings covering:
- Key exchange: `kRSA`, `kDHE`, `kECDHE`, `kPSK`, `kSRP`, `kGOST`
- Authentication: `aRSA`, `aDSS`, `aECDSA`, `aPSK`, `aGOST`
- Encryption: `AES`, `CHACHA20`, `CAMELLIA`, `ARIA`, `3DES`
- MAC: `SHA256`, `SHA384`, `GOST94`
- Protocol versions: `TLSv1`, `TLSv1.1`, `TLSv1.2`

### SRTP Stack Macros
Full typed stack (`STACK_OF(SRTP_PROTECTION_PROFILE)`) operations: `push`, `pop`, `find`, `insert`, `delete`, `free`, etc.

### Shutdown Flags
- `SSL_SENT_SHUTDOWN` (`1`)
- `SSL_RECEIVED_SHUTDOWN` (`2`)

## Usage Example

```c
#include <openssl/ssl.h>

/* Initialize SSL context with TLS method */
SSL_CTX *ctx = SSL_CTX_new(TLS_client_method());

/* Set cipher list using SSL_TXT constants */
SSL_CTX_set_cipher_list(ctx, SSL_TXT_ECDHE ":" SSL_TXT_AES ":" SSL_TXT_SHA256);

/* Create session and connect */
SSL *ssl = SSL_new(ctx);
SSL_set_fd(ssl, sockfd);
SSL_connect(ssl);

/* Check shutdown state */
int shut = SSL_get_shutdown(ssl);
if (shut & SSL_RECEIVED_SHUTDOWN) {
    SSL_shutdown(ssl);
}

SSL_free(ssl);
SSL_CTX_free(ctx);
```