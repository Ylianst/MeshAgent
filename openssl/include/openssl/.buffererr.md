<!-- source-hash: fe3de46e6b640be9cf58e0a02d6b5dae -->
Auto-generated OpenSSL header defining the error code infrastructure for the BUF (buffer) subsystem. It serves as a placeholder for BUF-specific reason codes used in OpenSSL error reporting.

## Key Components

- **Include guards** — `OPENSSL_BUFFERERR_H` / `#pragma once` prevent multiple inclusion
- **Dependencies**:
  - `opensslconf.h` — platform/build configuration
  - `symhacks.h` — symbol name compatibility shims
  - `cryptoerr_legacy.h` — legacy error macro compatibility layer
- **BUF reason codes section** — currently empty; populated by `util/mkerr.pl` when buffer-specific errors are defined

## Usage Example

```c
#include <openssl/buffererr.h>
#include <openssl/err.h>

/* BUF error reason codes would be referenced here if defined, e.g.: */
void check_buf_error(void) {
    unsigned long err = ERR_get_error();
    int lib    = ERR_GET_LIB(err);
    int reason = ERR_GET_REASON(err);

    /* Handle BUF subsystem errors */
    if (lib == ERR_LIB_BUF) {
        /* reason would match a BUF_R_* constant */
    }
}
```

> **Note:** This file is auto-generated by [`util/mkerr.pl`](https://github.com/openssl/openssl/blob/master/util/mkerr.pl). Do not edit it manually — changes will be overwritten. If you need to add BUF error reason codes, modify the upstream error definition files and regenerate.