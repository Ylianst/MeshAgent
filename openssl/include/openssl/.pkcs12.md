<!-- source-hash: fcaa73c5f369c8eeba01ba646177045b -->
OpenSSL PKCS#12 header providing data structures, constants, and function declarations for creating, parsing, and manipulating PKCS#12 (PFX) files — the standard format for bundling private keys with certificates.

## Key Components

### Types & Structures
- `PKCS12` — opaque top-level PFX container structure
- `PKCS12_SAFEBAG` — individual bag within a PKCS#12 safe (holds certs, keys, CRLs, secrets)
- `PKCS12_MAC_DATA` — MAC authentication data structure
- `PKCS12_BAGS` — low-level bag content container
- `STACK_OF(PKCS12_SAFEBAG)` — stack collection macros (`sk_PKCS12_SAFEBAG_*`)

### Key Constants
| Constant | Value | Purpose |
|---|---|---|
| `PKCS12_KEY_ID` | 1 | Key derivation ID |
| `PKCS12_IV_ID` | 2 | IV derivation ID |
| `PKCS12_MAC_ID` | 3 | MAC derivation ID |
| `PKCS12_MAC_KEY_LENGTH` | 20 | MAC key length in bytes |
| `PKCS12_DEFAULT_ITER` | (PKCS5) | Default KDF iteration count |

### Core Function Groups

**Safebag Accessors**
- `PKCS12_SAFEBAG_get1_cert()` / `PKCS12_SAFEBAG_get1_crl()` — extract X.509 cert/CRL
- `PKCS12_SAFEBAG_get0_p8inf()` / `PKCS12_SAFEBAG_get0_pkcs8()` — access private key info
- `PKCS12_SAFEBAG_get_nid()` / `PKCS12_SAFEBAG_get_bag_nid()` — query bag type

**Safebag Constructors**
- `PKCS12_SAFEBAG_create_cert()` / `PKCS12_SAFEBAG_create_crl()` — wrap cert/CRL
- `PKCS12_SAFEBAG_create_pkcs8_encrypt()` — create encrypted key bag
- `PKCS12_SAFEBAG_create_secret()` — create secret/arbitrary data bag

**Encryption/Decryption**
- `PKCS8_encrypt()` / `PKCS8_decrypt()` — encrypt/decrypt PKCS#8 private keys
- `PKCS12_decrypt_skey()` — decrypt a shrouded key bag
- `PKCS8_set0_pbe()` — set password-based encryption algorithm

**Packing/Unpacking**
- `PKCS12_pack_p7data()` / `PKCS12_unpack_p7data()` — pack safebags into PKCS#7
- `PKCS12_pack_p7encdata()` / `PKCS12_unpack_p7encdata()` — encrypted PKCS#7 content
- `PKCS12_pack_authsafes()` / `PKCS12_unpack_authsafes()` — top-level PFX authenticated safes

**Attribute Helpers**
- `PKCS12_add_localkeyid()` — set local key ID attribute
- `PKCS12_add_friendlyname_utf8()` / `PKCS12_add_friendlyname_asc()` — set human-readable name

## Usage Example

```c
#include <openssl/pkcs12.h>
#include <openssl/pem.h>

/* Parse a PKCS#12 file and extract certificate + private key */
int parse_pkcs12(const char *pfx_path, const char *password,
                 X509 **cert, EVP_PKEY **pkey) {
    FILE *fp = fopen(pfx_path, "rb");
    if (!fp) return 0;

    PKCS12 *p12 = d2i_PKCS12_fp(fp, NULL);
    fclose(fp);
    if (!p12) return 0;

    STACK_OF(X509) *ca_certs = NULL;
    int ret = PKCS12_parse(p12, password, pkey, cert, &ca_certs);
    PKCS12_free(p12);

    if (ca_certs)
        sk_X509_pop_free(ca_certs, X509_free);

    return ret;
}

/* Check MAC presence before parsing */
void inspect_pkcs12(const PKCS12 *p12) {
    if (PKCS12_mac_present(p12)) {
        const ASN1_OCTET_STRING *mac = NULL, *salt = NULL;
        const X509_ALGOR *macalg = NULL;
        const ASN1_INTEGER *iter = NULL;
        PKCS12_get0_mac(&mac, &macalg, &salt, &iter, p12);
        /* inspect MAC parameters */
    }
}
```

> **Note:** This header is auto-generated from `include/openssl/pkcs12.h.in`. Do not edit directly. For `_ex` variants (e.g., `PKCS12_SAFEBAG_get1_cert_ex`), pass an `OSSL_LIB_CTX` for explicit library context and property query string support.