<!-- source-hash: aa3bbf7e8644c1a6ae20dd728ab0218c -->
Auto-generated Microsoft Visual C++ resource header for the MeshMessenger Windows application, defining default ID counters used by the Visual Studio resource editor (AppStudio).

## Key Components

| Symbol | Value | Purpose |
|---|---|---|
| `_APS_NEXT_RESOURCE_VALUE` | `101` | Next available resource ID (dialogs, bitmaps, icons, etc.) |
| `_APS_NEXT_COMMAND_VALUE` | `40001` | Next available menu/command ID |
| `_APS_NEXT_CONTROL_VALUE` | `1001` | Next available dialog control ID |
| `_APS_NEXT_SYMED_VALUE` | `101` | Next available symbolic (named) resource ID |

## Usage Example

This file is included automatically by the `.rc` resource script and should not be edited manually. Visual Studio updates these counters each time a new resource is added via the resource editor:

```c
// MeshMessenger.rc (auto-references resource.h)
#include "resource.h"

// Example: a dialog resource would be assigned
// the current _APS_NEXT_RESOURCE_VALUE (101),
// then the counter increments automatically.
```

> **Note:** All symbols are guarded by `APSTUDIO_INVOKED` and `APSTUDIO_READONLY_SYMBOLS` preprocessor guards, ensuring these definitions are only active inside the Visual Studio resource editor — they have no effect on normal compilation.

This file is part of the MeshMessenger component within the [meshagent](https://github.com/flamingo-stack/meshagent) repository. It should be treated as auto-generated and committed as-is alongside `MeshMessenger.rc`.