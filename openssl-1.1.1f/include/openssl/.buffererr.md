<!-- source-hash: 1ec0c4c6a0188abe7c46a9baf597b878 -->
Auto-generated OpenSSL header that declares error-reporting infrastructure for the `BUF` (buffer memory) subsystem.

## Key Components

**Function**
- `ERR_load_BUF_strings()` — Registers the BUF error strings into the OpenSSL error system; must be called before BUF error codes are human-readable.

**Function Code Macros**

| Macro | Value | Associated Operation |
|---|---|---|
| `BUF_F_BUF_MEM_GROW` | 100 | `BUF_MEM_grow()` |
| `BUF_F_BUF_MEM_GROW_CLEAN` | 105 | `BUF_MEM_grow_clean()` |
| `BUF_F_BUF_MEM_NEW` | 101 | `BUF_MEM_new()` |

## Usage Example

```c
#include <openssl/buffer.h>
#include <openssl/err.h>
#include "buffererr.h"

int main(void) {
    /* Load BUF error strings for human-readable diagnostics */
    ERR_load_BUF_strings();

    BUF_MEM *buf = BUF_MEM_new();
    if (buf == NULL) {
        /* ERR_print_errors will now show BUF function/reason strings */
        ERR_print_errors_fp(stderr);
        return 1;
    }

    BUF_MEM_free(buf);
    return 0;
}
```

> **Note:** This file is auto-generated by `util/mkerr.pl` and should not be edited manually. It defines only function codes; no reason codes are defined for the BUF subsystem in this version.