<!-- source-hash: 03f0b24ae4b7f05456c79de20e821dc7 -->
Auto-generated OpenSSL error code header for the RAND (random number generation) subsystem, defining numeric constants for function codes and reason codes used in error reporting.

## Key Components

### Function Declaration
- **`ERR_load_RAND_strings()`** — Registers RAND error strings with the OpenSSL error system; must be called before RAND errors can produce human-readable messages.

### `RAND_F_*` — Function Codes
Numeric identifiers mapping to specific RAND functions for error attribution:

| Constant | Value | Associated Function |
|---|---|---|
| `RAND_F_RAND_BYTES` | 100 | `RAND_bytes()` |
| `RAND_F_RAND_DRBG_GENERATE` | 107 | DRBG generation |
| `RAND_F_RAND_DRBG_INSTANTIATE` | 108 | DRBG instantiation |
| `RAND_F_RAND_POOL_NEW` | 116 | Entropy pool creation |
| `RAND_F_RAND_LOAD_FILE` | 111 | File-based seeding |

### `RAND_R_*` — Reason Codes
Numeric identifiers describing why a RAND operation failed:

| Constant | Value | Meaning |
|---|---|---|
| `RAND_R_PRNG_NOT_SEEDED` | 100 | RNG lacks sufficient entropy |
| `RAND_R_DRBG_NOT_INITIALISED` | 104 | DRBG used before setup |
| `RAND_R_IN_ERROR_STATE` | 114 | DRBG in unrecoverable error state |
| `RAND_R_ENTROPY_INPUT_TOO_LONG` | 106 | Entropy exceeds allowed length |
| `RAND_R_SELFTEST_FAILURE` | 119 | DRBG self-test failed |

## Usage Example

```c
#include <openssl/rand.h>
#include <openssl/err.h>
#include "randerr.h"

unsigned char buf[32];

if (RAND_bytes(buf, sizeof(buf)) != 1) {
    unsigned long err = ERR_get_error();
    /* Reason code extracted from packed error */
    int reason = ERR_GET_REASON(err);

    if (reason == RAND_R_PRNG_NOT_SEEDED) {
        fprintf(stderr, "RNG not seeded — insufficient entropy\n");
    }
    ERR_print_errors_fp(stderr);
}
```

> **Note:** This file is auto-generated by `util/mkerr.pl`. Do not edit manually; regenerate via the OpenSSL build tooling when adding new error codes.