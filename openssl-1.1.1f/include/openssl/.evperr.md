<!-- source-hash: 3d1bf51d2d380a60f969cf33156d92be -->
Auto-generated OpenSSL EVP (Envelope) error codes header, defining numeric constants for function codes (`EVP_F_*`) and reason codes (`EVP_R_*`) used by the OpenSSL error reporting system.

## Key Components

**Function:** `ERR_load_EVP_strings(void)` — Registers human-readable EVP error strings into the OpenSSL error system at runtime.

**`EVP_F_*` constants** — Numeric identifiers for EVP functions where errors can originate (e.g., `EVP_F_EVP_CIPHERINIT_EX`, `EVP_F_EVP_PKEY_SIGN`, `EVP_F_EVP_DIGESTINIT_EX`).

**`EVP_R_*` constants** — Numeric reason codes describing the nature of an error (e.g., `EVP_R_BAD_DECRYPT`, `EVP_R_INVALID_KEY_LENGTH`, `EVP_R_UNSUPPORTED_CIPHER`).

## Usage Example

```c
#include <openssl/evp.h>
#include <openssl/err.h>

EVP_CIPHER_CTX *ctx = EVP_CIPHER_CTX_new();
if (!EVP_EncryptInit_ex(ctx, EVP_aes_256_cbc(), NULL, key, iv)) {
    /* Retrieve and print EVP error details */
    unsigned long err_code = ERR_get_error();
    int func   = ERR_GET_FUNC(err_code);  /* matches an EVP_F_* value */
    int reason = ERR_GET_REASON(err_code); /* matches an EVP_R_* value */
    ERR_print_errors_fp(stderr);
}
```

> **Note:** This file is auto-generated by `util/mkerr.pl` and should not be edited manually. Modifications belong in the OpenSSL source templates.