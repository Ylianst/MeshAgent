<!-- source-hash: 4ad27789915f06264461ad7156c2c98d -->
Assembly metadata file for the **WebRTC Sample** project, defining identity, versioning, and COM visibility attributes for the compiled assembly.

## Key Components

| Attribute | Value |
|---|---|
| `AssemblyTitle` | `WebRTC Sample` |
| `AssemblyDescription` | Basic sample for WebRTC data channel in C# |
| `AssemblyCompany` | Open Source under Apache 2.0 license |
| `AssemblyCopyright` | Copyright © 2014 Intel Corporation |
| `AssemblyVersion` | `0.1.0.0` |
| `AssemblyFileVersion` | `0.1.0.0` |
| `ComVisible` | `false` |
| `Guid` | `51a2007e-634e-4f21-8c0a-042af1269539` |

## Usage Example

Assembly attributes are applied at compile time and are not called directly. They can be read at runtime via reflection:

```csharp
using System.Reflection;

var assembly = Assembly.GetExecutingAssembly();
var title = assembly.GetCustomAttribute<AssemblyTitleAttribute>()?.Title;
var version = assembly.GetName().Version;

Console.WriteLine($"Assembly: {title} v{version}");
// Output: Assembly: WebRTC Sample v0.1.0.0
```

## Notes

- **COM Interop:** `ComVisible(false)` means assembly types are hidden from COM by default. Set `[ComVisible(true)]` on individual types to expose them selectively.
- **Versioning:** Currently at `0.1.0.0`, indicating an early/pre-release build.
- **License:** Apache 2.0 — see the repository for full license details: [flamingo-stack/meshagent](https://github.com/flamingo-stack/meshagent)