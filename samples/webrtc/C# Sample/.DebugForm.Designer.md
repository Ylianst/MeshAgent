<!-- source-hash: dbed909ebbb4e3f1c3ed60982ea69eb1 -->
Auto-generated Windows Forms designer file for the `DebugForm` partial class, defining the layout and initialization of UI controls used to display real-time WebRTC/SCTP diagnostic metrics.

## Key Components

| Control | Description |
|---|---|
| `sendRateLabel` | Displays current data send rate |
| `retryLabel` | Shows fast retry rate (styled in dark red) |
| `normalRetryLabel` | Shows normal retry rate |
| `rCreditsLabel` | Displays receiver credits |
| `windowSizeLabel` | Shows congestion window size |
| `fastRecoveryLabel` | Indicates fast recovery mode status |
| `t3Label` | Displays T3-RTX retransmission timer value |
| `rttLabel` | Shows round-trip time measurement |
| `tsnLabel` | Displays the TSN (Transmission Sequence Number) floor |

**Methods:**

- `InitializeComponent()` — Instantiates and positions all label controls within the 746×467 form
- `Dispose(bool disposing)` — Cleans up managed resources via the component container

## Usage Example

This file is auto-generated and should not be edited directly. The companion `DebugForm.cs` file interacts with these controls at runtime:

```csharp
// In DebugForm.cs - update labels with live SCTP diagnostics
public void UpdateStats(SctpStats stats)
{
    sendRateLabel.Text     = $"Send Rate: {stats.SendRate} kbps";
    retryLabel.Text        = $"Fast Retry Rate: {stats.FastRetryRate}";
    normalRetryLabel.Text  = $"Normal Retry Rate: {stats.NormalRetryRate}";
    rttLabel.Text          = $"Round Trip Time: {stats.Rtt} ms";
    tsnLabel.Text          = $"TSN Floor: {stats.TsnFloor}";
    windowSizeLabel.Text   = $"Congestion Window Size: {stats.CwndSize}";
    t3Label.Text           = $"T3-RTX Timer: {stats.T3Timer}";
    fastRecoveryLabel.Text = $"Fast Recovery Mode: {stats.FastRecovery}";
    rCreditsLabel.Text     = $"Receiver Credits: {stats.ReceiverCredits}";
}
```

> **Note:** Do not modify `InitializeComponent()` manually — use the Visual Studio Forms Designer to make layout changes.