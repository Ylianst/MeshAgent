<!-- source-hash: a455d118ed83d85a32aa7a2f0e3004fa -->
Auto-generated Windows Forms Designer file for the `SessionForm` partial class, defining the UI layout for a WebRTC session chat window.

## Key Components

| Member | Type | Description |
|---|---|---|
| `messageTextBox` | `TextBox` | Read-only, multiline display area for session messages with vertical scrollbar |
| `inputTextBox` | `TextBox` | User text input field; initially disabled until session is active |
| `sendButton` | `Button` | Triggers message send; initially disabled; docked to the right of the input panel |
| `panel1` | `Panel` | Bottom-docked container housing `inputTextBox` and `sendButton` |
| `InitializeComponent()` | `void` | Designer-generated method wiring up all control properties and event handlers |
| `Dispose(bool)` | `override void` | Releases managed resources held by the component container |

**Event bindings configured:**
- `inputTextBox.TextChanged` → `inputText_TextChanged`
- `inputTextBox.KeyUp` → `inputText_KeyUp`
- `sendButton.Click` → `sendButton_Click`
- `sendButton.KeyUp` → `inputText_KeyUp`
- `FormClosing` → `UserForm_FormClosing`

## Usage Example

This file is auto-managed by the Visual Studio Designer. The companion `SessionForm.cs` implements the event handlers referenced here:

```csharp
// SessionForm.cs (partial class companion)
public partial class SessionForm : Form
{
    public SessionForm()
    {
        InitializeComponent(); // Calls designer-generated setup
    }

    private void sendButton_Click(object sender, EventArgs e)
    {
        // Send message logic using inputTextBox.Text
    }

    private void inputText_TextChanged(object sender, EventArgs e)
    {
        // Enable/disable sendButton based on input
        sendButton.Enabled = inputTextBox.Text.Length > 0;
    }
}
```

> **Note:** Do not manually edit this file. Modify the form layout through the Visual Studio Designer instead. Source: [`SessionForm.Designer.cs`](https://github.com/flamingo-stack/meshagent/blob/main/SessionForm.Designer.cs)