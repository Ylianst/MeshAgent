<!-- source-hash: 9baa77ca4036ed0048156fe2d6528d1c -->
Auto-generated Windows Forms Designer file that defines the UI layout and control initialization for the `StunSettingsForm` dialog, used to configure STUN server settings in a WebRTC application.

## Key Components

| Control | Type | Purpose |
|---|---|---|
| `stunTextBox` | `TextBox` | Multiline input for entering STUN server addresses |
| `stunCheckBox` | `CheckBox` | Toggles STUN server usage on/off (checked by default) |
| `label1` | `Label` | Descriptive text explaining STUN server purpose |
| `groupBox1` | `GroupBox` | Groups the STUN checkbox and text box under "STUN Servers" |
| `okButton` | `Button` | Confirms settings (`DialogResult.OK`) |
| `cancelButton` | `Button` | Dismisses the dialog without saving (`DialogResult.Cancel`) |

## Usage Example

This file is consumed automatically by the companion `StunSettingsForm.cs` file. The form is typically instantiated and shown modally:

```csharp
using (var form = new StunSettingsForm())
{
    if (form.ShowDialog(this) == DialogResult.OK)
    {
        // Retrieve configured STUN servers from form
        var stunServers = form.StunServers;
    }
}
```

## Notes

- **Do not edit this file manually** — it is regenerated by the Visual Studio Designer. Logic and event handlers (`stunCheckBox_CheckedChanged`, `okButton_Click`, `cancelButton_Click`) are implemented in the partial class counterpart `StunSettingsForm.cs`.
- The form is fixed-size (non-resizable via maximize/minimize) and centered relative to its parent.
- `okButton` is set as `AcceptButton` (Enter key) and `cancelButton` as `CancelButton` (Escape key).