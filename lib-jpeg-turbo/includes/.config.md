<!-- source-hash: 6223d2eabf1f6e027c7d4314dd089677 -->
Auto-generated configuration header for the libjpeg-turbo 1.5.2 build, produced by `autoconf`/`configure` to capture platform capabilities and build-time settings.

## Key Components

| Macro | Value | Purpose |
|---|---|---|
| `LIBJPEG_TURBO_VERSION` | `1.5.2` | Human-readable library version |
| `LIBJPEG_TURBO_VERSION_NUMBER` | `1005002` | Integer-encoded version for comparisons |
| `JPEG_LIB_VERSION` | `62` | libjpeg API compatibility level |
| `BUILD` | `"20161004"` | Build date stamp |
| `C_ARITH_CODING_SUPPORTED` | `1` | Arithmetic encoding enabled |
| `D_ARITH_CODING_SUPPORTED` | `1` | Arithmetic decoding enabled |
| `WITH_SIMD` | `1` | SIMD acceleration enabled |
| `MEM_SRCDST_SUPPORTED` | `1` | In-memory source/destination managers enabled |
| `INLINE` | `inline __attribute__((always_inline))` | Forced inlining hint |
| `SIZEOF_SIZE_T` | `8` | 64-bit platform (`size_t` = 8 bytes) |

Standard POSIX headers (`dlfcn.h`, `unistd.h`, `stdint.h`, etc.) and C runtime functions (`memcpy`, `memset`) are all confirmed present.

## Usage Example

Conditionally compile based on detected capabilities:

```c
#include "config.h"

/* Guard SIMD-accelerated path */
#ifdef WITH_SIMD
    use_simd_dct();
#else
    use_scalar_dct();
#endif

/* Version gating */
#if LIBJPEG_TURBO_VERSION_NUMBER >= 1005000
    /* Use features introduced in 1.5.x */
    enable_mem_src_dst();
#endif

/* Force-inline a hot function */
static INLINE void process_block(JSAMPLE *data) {
    /* ... */
}
```

> **Note:** This file is auto-generated by `configure` from `config.h.in` — do not edit manually. Regenerate by running `./configure` in the source tree. Source: [`config.h`](https://github.com/flamingo-stack/meshagent/blob/main/config.h)