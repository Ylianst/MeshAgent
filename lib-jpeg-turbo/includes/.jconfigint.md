<!-- source-hash: 779706da06b8ed6450aed6312a62dbc8 -->
Internal build configuration header for libjpeg-turbo, auto-generated by `configure` from `jconfigint.h.in`. Defines compile-time constants for build metadata, compiler hints, and platform sizing used across the library internals.

## Key Components

| Macro | Value | Purpose |
|---|---|---|
| `BUILD` | `"20161004"` | Build date stamp |
| `INLINE` | `inline __attribute__((always_inline))` | Forces GCC/Clang inlining |
| `PACKAGE_NAME` | `"libjpeg-turbo"` | Full library name |
| `VERSION` | `"1.5.2"` | Package version string |
| `SIZEOF_SIZE_T` | `8` | Platform pointer width in bytes (64-bit) |

## Usage Example

```c
#include "jconfigint.h"

/* Use forced inlining for a hot path function */
INLINE int fast_clamp(int val, int min, int max) {
    if (val < min) return min;
    if (val > max) return max;
    return val;
}

/* Runtime version check */
void print_build_info(void) {
    printf("%s version %s (build %s)\n", PACKAGE_NAME, VERSION, BUILD);
}

/* Size-conditional logic */
#if SIZEOF_SIZE_T == 8
    /* 64-bit specific optimizations */
#else
    /* 32-bit fallback */
#endif
```

## Notes

- **Do not edit manually** — this file is regenerated by `./configure` from `jconfigint.h.in`
- `SIZEOF_SIZE_T 8` indicates this was configured for a **64-bit** target platform
- `INLINE` uses GCC's `always_inline` attribute; on MSVC builds this would typically map to `__forceinline`
- This is an **internal** header; it is not part of the public libjpeg API surface